    <li>
      <%= gravatar_for user, size: 52 %>
      <%= link_to user.name, user %>
      <% if current_user.admin? && !current_user?(user) %>
      	<ra>
      	<%= link_to "Delete", user, method: :delete, confirm: "You sure?", class: "btn btn-small btn-primary" %>
      	</ra>
      <% end %>
    </li>